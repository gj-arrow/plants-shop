# Sitemap для plant-shop — план реализации

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Добавить динамический `/sitemap.xml` для поисковой индексации сайта (Google, Яндекс).

**Architecture:** Роут `src/app/sitemap.ts` по конвенции Next.js (metadata route). Функция запрашивает товары из MySQL через существующий `queryAll` из `src/lib/db.ts` и возвращает массив URL: главная + `/products/{id}`. `export const dynamic = 'force-dynamic'` — роут не пререндерится при локальной сборке, а исполняется в рантайме на хостинге с реальной БД.

**Tech Stack:** Next.js 16 (App Router, metadata routes), TypeScript, MySQL (mysql2/promise).

## Global Constraints

- Домен захардкожен: `https://xn----ctbhcrqcg4cxb8cg8a.xn--90ais` (punycode от `цветы-людмилы.бел`).
- Включаются **все** товары, включая `out_of_stock = 1` (решение пользователя).
- Исключаются: `/admin/*`, `/api/*`, `/login`, `/favorites`.
- У проекта **нет тестового фреймворка** (см. AGENTS.md) — верификация ручная через dev-сервер, `curl`, lint и production-сборку.
- Путь-алиас `@/*` = `./src/*`. Всё в стиле существующего кода (точки с запятой, двойные кавычки отсутствуют — в проекте одинарные).

---

### Task 1: Создать `src/app/sitemap.ts`

**Files:**
- Create: `src/app/sitemap.ts`

**Interfaces:**
- Consumes: `queryAll<T>(sql: string, params?: any[]): Promise<T[]>` из `src/lib/db.ts` (существующая, сигнатура на строке 21).
- Produces: роут `GET /sitemap.xml` с XML-ответом. Тип `MetadataRoute.Sitemap` (Next.js 16).

- [ ] **Step 1: Создать файл**

```ts
import type { MetadataRoute } from 'next';
import { queryAll } from '@/lib/db';

// Punycode-форма домена цветы-людмилы.бел
const SITE_URL = 'https://xn----ctbhcrqcg4cxb8cg8a.xn--90ais';

// Не генерировать статически при сборке — запрашивать БД хостинга в рантайме
export const dynamic = 'force-dynamic';

export default async function sitemap(): Promise<MetadataRoute.Sitemap> {
  const products = await queryAll<{ id: number; created_at: Date | null }>(
    'SELECT id, created_at FROM products'
  );

  const urls: MetadataRoute.Sitemap = [
    {
      url: `${SITE_URL}/`,
    },
  ];

  for (const product of products) {
    urls.push({
      url: `${SITE_URL}/products/${product.id}`,
      ...(product.created_at ? { lastModified: product.created_at } : {}),
    });
  }

  return urls;
}
```

- [ ] **Step 2: Проверить в dev-режиме**

Локальный MySQL должен быть запущен (переменная `DATABASE_URL` в `.env.local`). В терминале 1:

```bash
npm run dev
```

В терминале 2:

```bash
curl -s http://localhost:3000/sitemap.xml
```

Ожидаемо: XML-ответ с `<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">`, содержащий `<loc>https://xn----ctbhcrqcg4cxb8cg8a.xn--90ais/</loc>` и по `<loc>.../products/{id}</loc>` на каждый товар из локальной БД. Если в БД нет товаров — только главная.

- [ ] **Step 3: Проверить линтер**

```bash
npm run lint
```

Ожидаемо: без ошибок (без изменений, связанных с новым файлом).

- [ ] **Step 4: Проверить production-сборку**

```bash
npm run build
```

Ожидаемо: сборка успешна. Важно: из-за `force-dynamic` sitemap **не** пререндерится на этапе сборки, поэтому локальная БД при сборке не нужна, и отсутствие строки `/sitemap.xml` в списке статически сгенерированных роутов — нормально.

- [ ] **Step 5: Закоммитить**

```bash
git add src/app/sitemap.ts
git commit -m "feat: динамический sitemap.xml для поисковой индексации"
```